import Button from "../components/Button";
import SubmitNote from "../components/SubmitNote";

export default function HomePage() {
  return (
    <>
      <h1 className="test">{`hey y'all this is a notepad`}</h1>
      <p>{`pleas treat her gently her code is fragil as eff`}</p>
      <p>{`y is it "not found" when you hit f5 on a non-root url`}</p>
      <p>{`y does the submitnote component not work when added to the homepage`}</p>
      <p>{`what's with that split() problem in notes.jsx`}</p>
      <p>{`why did all of this take me so long for such low quality`}</p>
      <p>{`i am losing my mind`}</p>
      {/* <SubmitNote className={`w-128 h-128 max-w-screen`} /> */}
    </>
  );
}
